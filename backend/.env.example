# 服务器配置
PORT=3000
NODE_ENV=development

# 数据库配置
MONGODB_URI=mongodb://localhost:27017/recipeapp

# JWT密钥（用于用户认证）
JWT_SECRET=your-secret-key-change-in-production

# 阿里云 通义千问 + 百炼（菜谱生成、每日灵感）
DASHSCOPE_API_KEY=your-dashscope-api-key
# qwen-turbo | qwen-plus | qwen3.5-plus | qwen-max 等
QWEN_MODEL=qwen3.5-plus

# 录音文件识别（ASR）选择：dashscope（百炼 Paraformer）| isi2（智能语音交互 2.0）
# ASR_PROVIDER=dashscope 时使用 DASHSCOPE_API_KEY + ASR_MODEL
# ASR_PROVIDER=isi2 时使用智能语音 2.0，需配置 NLS_APPKEY，AK/SK 可与 OSS 共用
# ASR_PROVIDER=isi2
# ASR_MODEL=paraformer-v2
# NLS_APPKEY=your-nls-appkey
# NLS_REGION=cn-shanghai
# ALIYUN_ACCESS_KEY_ID=   # 不填则使用 OSS_ACCESS_KEY_ID
# ALIYUN_ACCESS_KEY_SECRET=

# 微信小程序配置
WECHAT_APPID=your-wechat-appid
WECHAT_SECRET=your-wechat-secret

# 视频解析服务配置（推荐配置，返回 media_url 时可用于 ASR 转写）
# 支持的 API 类型: custom（自定义）, yaohu（妖狐数据）, litchi（Litchi API）, ucmao（开源方案）
VIDEO_PARSER_API_KEY=your-video-parser-api-key  # ucmao 等开源方案可能不需要
VIDEO_PARSER_API_URL=https://api.example.com/video/parse
VIDEO_PARSER_API_TYPE=custom

# 示例配置（parse-ucmao-backend 开源方案，推荐）:
# VIDEO_PARSER_API_KEY=  # ucmao 不需要 API Key，留空即可
# VIDEO_PARSER_API_URL=http://localhost:5000/api/parse
# VIDEO_PARSER_API_TYPE=ucmao

# 示例配置（妖狐数据）:
# VIDEO_PARSER_API_KEY=your-yaohu-key
# VIDEO_PARSER_API_URL=https://api.yaohud.cn/api/video/parse
# VIDEO_PARSER_API_TYPE=yaohu

# 示例配置（Litchi API）:
# VIDEO_PARSER_API_KEY=your-litchi-key
# VIDEO_PARSER_API_URL=https://api.litchi-ai.com/v1/parse
# VIDEO_PARSER_API_TYPE=litchi

# 示例配置（yt-dlp 本地解析，推荐用于「必须识别视频内容」场景）:
# 需同时安装 yt-dlp 与 ffmpeg（Mac: brew install yt-dlp ffmpeg）
# VIDEO_PARSER_API_TYPE=ytdlp
# YT_DLP_PATH=/usr/local/bin/yt-dlp
# FFMPEG_PATH=/usr/local/bin/ffmpeg

# 阿里云 OSS（与 yt-dlp 配合：下载音频 → 上传 OSS → 得到可访问 URL 供 ASR 转写，实现基于视频内容的菜谱生成）
# 不配置则不会走「下载+上传」流程，仅用解析到的标题/描述生成菜谱（不符合「必须识别视频内容」要求）
# OSS_REGION=oss-cn-hangzhou
# OSS_BUCKET=your-bucket-name
# OSS_ACCESS_KEY_ID=your-access-key-id
# OSS_ACCESS_KEY_SECRET=your-access-key-secret
